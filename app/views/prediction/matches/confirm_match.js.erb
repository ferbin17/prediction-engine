<% if @match %>
  $(`#match-<%= @match.id %>-user-<%= @current_user.id %>`).replaceWith( `<%= render partial: "match",
      locals: {match: @match, mode: true} %>`);
<% end %>
$('#errors').html(`<%= render partial: "layouts/prediction/errors", locals: {errors: @errors} %>`);
