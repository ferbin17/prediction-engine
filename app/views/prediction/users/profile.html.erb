<div class="container inner-container user-profile-container">
  <div class="album">
    <div class="container">
      <div class="row g-3" id="userProfile">
        <div class="col">
          <div class="card shadow-sm">
            <div class="card-body">          
              <%= render partial: "user_details", locals: {user: @user} %>
              
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to t(:edit_details), edit_user_path(id: @user.id),
                      class: "btn btn-sm btn-outline-secondary" %>
                  <%= link_to t(:change_password), change_password_user_path(id: @user.id),
                      class: "btn btn-sm btn-outline-secondary" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>