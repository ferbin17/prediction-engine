<% if @phase && @phase.is_deleted %>
  $(`#phase-<%= @phase.id %>-user-<%= @current_user.id %>`).remove();
<% else %>
  $('#errors').html(`<%= render partial: "layouts/prediction/errors", locals: {errors: @errors} %>`);
<% end %>