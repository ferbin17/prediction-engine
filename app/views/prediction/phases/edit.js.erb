<% if @phase.present? %>
  $('#form-modal').html(`<%= render partial: "phase_form",
    locals: {phase: @phase} %>`);
  $('#form-modal').show();
<% else %>
  $('#errors').html(`<%= render partial: "layouts/prediction/errors", locals: {errors: @errors} %>`);
<% end %>